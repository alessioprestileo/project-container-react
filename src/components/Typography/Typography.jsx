// @flow

import React from 'react';
import TypographyImported from '@material-ui/core/Typography';

const Typography = (props: Object) => <TypographyImported {...props} />;

export default Typography;
