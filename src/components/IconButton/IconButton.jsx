// @flow

import React from 'react';
import IconButtonImported from '@material-ui/core/IconButton';

const IconButton = (props: Object) => <IconButtonImported {...props} />;

export default IconButton;
