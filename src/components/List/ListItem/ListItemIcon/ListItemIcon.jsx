// @flow

import React from 'react';
import ListItemIconImported from '@material-ui/core/ListItemIcon';

const ListItemIcon = (props: Object) => <ListItemIconImported {...props} />;

export default ListItemIcon;
