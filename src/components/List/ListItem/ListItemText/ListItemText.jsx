// @flow

import React from 'react';
import ListItemTextImported from '@material-ui/core/ListItemText';

const ListItemText = (props: Object) => <ListItemTextImported {...props} />;

export default ListItemText;
